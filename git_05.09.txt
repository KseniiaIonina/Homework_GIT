
что такое ветка в GIT;

Ветка в Git — это простой перемещаемый указатель на один из таких коммитов.

какое отношение имеют ssh-ключи к GITHUB;
Проведя аутентификацию по ключам SSH один раз на гитхабе, более не надо залогиниваться

что позволяет делать сервис GitHub Pages.

Позволяет опубликовать свой сайт и сделать доступным


Приведите команды, которые выполняют след. действия (GIT):


создание, удаление веток;
Git branch name
Git branch -d name

переключение между ветками;
Git checkout name

загрузка и удаление веток на GitHub.
git remote add origin name git-repozitoriy
git push -u origin master (если проект уже загружен)

Коротко опишите, каким образом происходит публикация статического веб-сайта с помощью GitHub Pages.
Через настройки в page, выбрав соответствующую ветку